Public Class NTalle
    'Function agregar_talle(descripcion As Integer) As Boolean
    '    Dim otalle = New talle
    '    With otalle
    '        .descripcion = descripcion
    '        .estado_talle = 1
    '    End With
    '    Dim objdtalle = New DTalle
    '    If objdtalle.Dguardar_talle(otalle) Then
    '        Return True
    '    Else
    '        Return False
    '    End If

    'End Function
    'Public Sub cargarGrid(grid As DataGridView)
    '    Dim tal As DTalle = New DTalle
    '    grid.DataSource = tal.getTallesAll()
    '    grid.Columns(0).HeaderText = "ID"
    '    grid.Columns(1).HeaderText = "Descripción"
    '    grid.Columns(2).HeaderText = "Estado"
    '    grid.Columns(3).Visible = False
    'End Sub

End Class

